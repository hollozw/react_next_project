'use client'

import { useEffect } from "react";


const Home = ({ detailPhotos }: any) => {
  useEffect(() => {
  }, [])
  return (
    <div className="w-full h-full">
      children
    </div>
  );
};

export default Home;
